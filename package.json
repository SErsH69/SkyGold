{
  "name": "skygold",
  "version": "1.0.0",
  "description": "1. npm init\r 2. npm install node-sass\r 3. package.json => scripts => \"dev\": \"node-sass --watch scss -o css\"\r 4. npm run dev",
  "main": "index.js",
  "scripts": {
    "dev": "node-sass --watch scss -o css"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^7.0.1"
  }
}
